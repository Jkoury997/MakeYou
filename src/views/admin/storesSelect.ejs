<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head") %>
    <title>Dashboard</title>
</head>
<body>
    <%- include("./partials/sidebar") %>
    
    <main class="container-fluid" style="padding-left: 25%; ">
        <div id="mainContent">
            <p>Selecciona una tienda para poder empezar a preparar.</p>
                <form action="/admin/stores/pickList" method="post">
                    <div class="d-grid gap-2 col-6 mx-auto">
                        <% stores.forEach((store) => { %>
                            <button class="btn btn-primary" type="submit" name="id" value="<%= store.Codigo %>"><%= store.Descripcion %></button>
                        <% }); %>
                    </div>
                </form>
        </div>
    </main>
</body>
</html>